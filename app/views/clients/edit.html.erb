<%= @title = "Editing #{@client.society_name}" %>

<%= render 'form' %>

<% @links = [link_to('Show', event_client_path(@client, :event_id => @event)),
             link_to('Back', event_path(@event))] %>
