<%= form_tag(new_client_form_path(:client_id => client), :method => :get) do %>

  <%= hidden_field_tag 'client', client.id %>
  <%= select_tag 'type', options_for_select(types.keys) %>
  <%= submit_tag 'New Form' %>
<% end %>
