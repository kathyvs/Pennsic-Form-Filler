<%= form_tag(new_event_client_form_path(:event_id => event, :client_id => client), :method => :get) do %>

  <%= select_tag 'type', options_for_select(form_types) %>
  <%= submit_tag 'New Form' %>
<% end %>
