<% @title = "Editing #{@form.type_name} form for #{@client.society_name}" %>

<%= render 'form' %>

<% links = [
     link_to('Show', form_path(@form)),
     link_to('Back',  event_client_path(@client, :event_id => @event))] %>
